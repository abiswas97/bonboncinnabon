---
description: Recompute the rest of today's blocks from the current time
---

Invoke the `reschedule` skill. It recomputes today's remaining blocks from now,
around the remaining Google Calendar commitments, never silently dropping a must,
and updates TickTick after you confirm.

$ARGUMENTS
