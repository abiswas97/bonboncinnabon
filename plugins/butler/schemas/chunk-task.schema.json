{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/abiswas97/bonboncinnabon/blob/main/plugins/butler/schemas/chunk-task.schema.json",
  "version": "0.3.1",
  "$comment": "Versioned with the butler plugin release (plugins/butler/CHANGELOG.md). Last schema change: 0.3.1 (priority disambiguation, reminder.triggers array).",
  "title": "Butler chunk task",
  "description": "Logical shape of a chunk — a child task (one focused sitting) under a parent work unit. The agent builds this, then maps it onto TickTick fields per references/template.md. Real subtask (parentId), never a checklist item, so it can carry its own dates/reminders/focus estimate.",
  "type": "object",
  "required": ["id", "title", "context", "kind", "ai_generated"],
  "additionalProperties": false,
  "if": { "properties": { "context": { "const": "work" } }, "required": ["context"] },
  "then": { "required": ["stage", "intensity", "ai_discount", "est0_min"] },
  "else": { "$comment": "Personal chunks SHALL NOT carry the work axes.", "properties": { "stage": false, "intensity": false, "ai_discount": false } },
  "properties": {
    "id": {
      "type": "string",
      "description": "Stable local id used by the packer."
    },
    "context": {
      "enum": ["work", "personal"],
      "description": "The task's context (derived from its TickTick project, confirmed with the user). work = full pipeline model (stage/intensity/ai_discount/est0 required); personal = lightweight (priority + est0 + reminder, no work axes). A chunk read with NO context field is treated as `work` for 0.2.0 back-compat — that defaulting is a reader rule applied by the skills, so newly generated chunks MUST set this field."
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 70,
      "description": "Stage label + short qualifier, sentence case. e.g. 'Backend: pricer API + carry-back'. No ticket prefix, no emoji, no trailing period. Code identifiers verbatim. The concrete first action lives in the body, not the title. See task-contract.md."
    },
    "stage": {
      "enum": ["research", "db", "backend", "frontend", "review", "address-comments", "qa", "deploy"],
      "$comment": "SSOT for these stage keys is config.yaml `contexts.work.pipeline`; keep in sync (also mirrored in packer-input.schema.json).",
      "description": "Work context only. Pipeline stage (see config.yaml `contexts.work.pipeline`). The decomposition unit, the calibration reference class, and the SINGLE SOURCE for the derived activity bucket the packer clusters by. Not a TickTick tag. Intake picks applicable stages and may add ad-hoc chunks for off-pipeline work."
    },
    "intensity": {
      "enum": ["deep", "shallow"],
      "description": "Work context only. Freshness axis: the ONE hand-set work-shape tag. deep = needs a fresh slot; shallow = light. Defaults come from the stage (config `contexts.work.pipeline`) but are overridable per chunk. A deep code review or verifying coupled AI output is `deep` — judge the focus it demands, not the activity."
    },
    "ai_discount": {
      "enum": ["discounted", "none", "premium"],
      "description": "Work context only. Defaults from the stage; override per chunk. discounted = AI compresses this (build on understood code, mechanical refactor, codemod). none/premium = AI does not reliably help and may slow you (ambiguous-spec resolution, cross-system trace/debug, decisions needing people, reviewing AI output). Used for estimation; calibration crosses stage x ai_discount for research/backend."
    },
    "est0_min": {
      "type": "integer",
      "minimum": 1,
      "description": "IMMUTABLE original focus-minute estimate, set once at intake and never overwritten on reschedule. Rendered in the description as `~<n>m` (not the old `est0:` label); the anchor for calibration (compare against actual focus time, grouped by stage from the title). Required for work chunks; optional for personal."
    },
    "priority": {
      "enum": ["must", "should", "want", null],
      "$comment": "Commitment layer (the must/should/want TAG), NOT TickTick's numeric priority. Distinct from parent-task.schema.json `priority` (none/low/medium/high -> 0/1/3/5). Maps to the commitment tag and to packer-input `priority`.",
      "description": "Commitment tag, set only when the chunk is scheduled for the target day; null otherwise."
    },
    "why": { "type": ["string", "null"], "maxLength": 120, "description": "Deprecated in the rendered description (lean form drops the Why line). Kept for back-compat; not written to new tasks." },
    "where": { "type": ["array", "null"], "items": { "type": "string" }, "description": "Concrete locations (file / symbol / endpoint) that seed the first-action opening line of the description; rendered as prose, no `Where:` label." },
    "done": { "type": ["string", "null"], "maxLength": 120, "description": "ONE observable acceptance signal, rendered as a `Done when …` line; omitted when the title implies success. More than one signal → split into subtasks, never a checklist." },
    "ref": { "type": ["string", "null"], "description": "Source ticket (PR / issue) — rendered as a `[TICKET ↗](url)` link where detail/AC live. Personal chunks omit it." },
    "scheduled": {
      "type": "boolean",
      "default": false,
      "description": "True only for the target day's chunks. Scheduled chunks get start/due, timeZone, reminder, and a pomo estimate; all others stay dateless."
    },
    "block": {
      "type": ["object", "null"],
      "properties": {
        "start": { "type": "string", "format": "date-time" },
        "end": { "type": "string", "format": "date-time" }
      },
      "description": "From the packer output; maps to TickTick startDate/dueDate."
    },
    "pomo_estimate": { "type": ["integer", "null"], "minimum": 1, "description": "Work context only. Maps to TickTick focusSummaries[].estimatedPomo. Personal chunks bypass the packer and carry no pomo estimate." },
    "reminder": {
      "type": ["object", "null"],
      "additionalProperties": false,
      "properties": {
        "due": { "type": "string", "format": "date-time", "description": "Personal due time; maps to TickTick dueDate (isAllDay:false) and is the reminders' date anchor. Chosen conversationally, or derived from a live read of the day on defer." },
        "triggers": { "type": "array", "items": { "type": "string" }, "minItems": 1, "description": "TRIGGER strings, e.g. ['TRIGGER:PT0S'] (at due) or ['TRIGGER:-PT30M','TRIGGER:PT0S']. Maps directly to TickTick `reminders[]`, which is itself an array." }
      },
      "required": ["due", "triggers"],
      "description": "Light personal scheduling: a due time + reminder. Personal chunks bypass the packer entirely (no block, no pomo, no intensity). Set only when the personal chunk is scheduled for the target day."
    },
    "ad_hoc": {
      "type": "boolean",
      "default": false,
      "description": "True when this chunk is off-pipeline (no matching stage) — the hybrid escape hatch. Stage still required; pick the closest, or 'backend' as a neutral default."
    },
    "parent_ticket": {
      "type": ["string", "null"],
      "description": "The parent work unit's ticket (or a local ref when there is no ticket). Resolved to TickTick parentId at write time."
    },
    "kind": { "const": "TEXT" },
    "ai_generated": { "const": true, "description": "Always true; drives the `ai` tag." }
  }
}
