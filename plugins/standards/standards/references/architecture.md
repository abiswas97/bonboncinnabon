# Architecture

- Name the domain boundary and its owner before adding a dependency.
- Keep infrastructure details behind explicit interfaces.
- Prefer one canonical model with generated or translated host views.
- Search for an existing owner before introducing parallel behavior.
- Generalize only when the common contract is already evidenced.
